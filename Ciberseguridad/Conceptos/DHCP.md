# DHCP (Dynamic Host Configuration Protocol)
Es un protocolo de red que permite la asignación automática de direcciones IP sin necesidad de ser configuradas de manera manual en cada dispositivo. Esto permite a su vez la reutilización de direcciones [IP](IP) cuando un dispositivo se desconecta.

En redes corporativas, el servidor DHCP suele actualizar automáticamente los registros del servidor [DNS](DNS) interno cuando un equipo se conecta. Esto se llama **Dynamic DNS (DDNS)** y permite que puedas encontrar el equipo de un compañero por su nombre (ej: `pc-juan.empresa.local`) aunque su [Dirección IP](IP) cambie.
## Puertos
- **Puerto:** 67 (Servidor)
- **Puerto:** 68 (Cliente)